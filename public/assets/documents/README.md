# Documents

Place viewable/downloadable documents here.

- `resume/resume.pdf` — referenced by `portfolio.json` documents[id=resume].

Paths are configured in `portfolio.json`, never hardcoded in components.
